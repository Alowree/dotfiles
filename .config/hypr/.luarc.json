{
  // We ship autogenerated Lua stubs in the repo’s meta/ directory.
  // You can set up your LSP to automatically load them.
  // This file is usually installed in /usr/share/hypr/stubs/.

  // Tell me what this is about.

  // Why some vloger use "/home/username/.config/hypr" instead?
  // What's the difference?
  "workspace": {
    "library": ["/usr/share/hypr/stubs"]
  },
  "diagnostics": {
    "globals": ["hl"]
  }
}
